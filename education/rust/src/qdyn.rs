pub struct QuantumDynamics {
    pub d: usize, pub iters: usize, pub nstate: usize, pub points: usize, pub dt: f64, pub mass: f64, pub rmax: f64, pub rmin: f64, pub imaginary: bool, pub savewfn: bool
}
